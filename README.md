# PRSK_Editor

PRSK Editor 是为了规范化PJS字幕组内翻译流程而开发的工具。

## 使用

请自行下载requirements.txt中的依赖库，使用

```bash
python main.pyw
```

运行程序。

你也可以使用PyInstaller将程序打包成exe文件：

```bash
pyinstaller --onefile main.pyw
```